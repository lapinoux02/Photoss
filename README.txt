LANCER LE SERVEUR :
	Lancer la commande bash (à la racine du projet front) : 'http-server -p 8000'

RENSEIGNER L'ADRESSE DU BACKEND :
	Modifier la variable 'BASE_URL' dans le fichier 'utils/utils.js'.
	Un reverseProxy est installé pour rediriger correctement les requêtes au back.